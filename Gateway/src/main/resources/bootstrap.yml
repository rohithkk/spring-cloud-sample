spring:
   application:
      name: gateway
   cloud:
      config:
        name: gateway
        uri: http://localhost:8888
        #fail on startup if the client cannot connect to the config server
        fail-fast: true
        username: user
        password: password
   profiles:
      active: dev