spring:
   application:
      name: config-server

server:
   port: 8081

spring:
   cloud:
      config:
         server:
            git:
               uri: https://github.com/rohithkk/spring-cloud-sample/config                  