spring:
   application:
      name: users
   cloud:
      config:
        uri: http://localhost:8888
        #fail on startup if the client cannot connect to the config server
        fail-fast: true
   profiles:
      active: dev
